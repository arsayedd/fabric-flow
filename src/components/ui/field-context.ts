import { createContext } from "react";

/**
 * رقم الخانة اللي `Field` بيولّده، عشان `Input` يربط نفسه بالِيبل لوحده.
 *
 * لوحة Issues في كروم كانت بتقول حاجتين على شاشة الدخول: لِيبل مالوش
 * `for` ومش لافف خانته، وخانات مالهاش `id` ولا `name`. ودي مش ملاحظات
 * شكلية — لِيبل مش مربوط معناه إن الضغط عليه مابيفتحش الخانة والقارئ
 * الصوتي مابيقولش اسمها، وخانة من غير `name` مدير كلمات السر في المتصفح
 * مابيحفظهاش.
 *
 * والربط بيحصل هنا في `Field` مش في كل صفحة، لأن `Field` مستخدم في ٢٩٧
 * مكان — فأي حل بالإيد كان هيتنفّذ في تسعين مكان ويتنسى في الباقي.
 *
 * وهي في ملف لوحدها عشان `Input` في `components/ui/` و`Field` في
 * `components/AuthShell.tsx` — ولو اتبادلوا الاستيراد بيبقى فيه دورة.
 */
export const FieldIdContext = createContext<string | null>(null);
